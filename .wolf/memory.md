# Memory

> Chronological action log. Hooks and AI append to this file automatically.
> Old sessions are consolidated by the daemon weekly.

## Session: 2026-08-08 15:02

| Time | Action | File(s) | Outcome | ~Tokens |
|------|--------|---------|---------|--------|
| 15:10 | Saved REEBAU website content into persistent AI-context file (source: gitignored `.local/contant.md`) | `.wolf/reebau-content.md` | done | ~950 |
| 15:20 | Recorded confirmed page structure (Home/Services/Galerie/Über uns/Kontakt + Impressum/AGB/Datenschutz), dynamic Services detail routes, and "Services" terminology decision | `.wolf/STATUS.md`, `.wolf/reebau-content.md`, `.wolf/cerebrum.md` | done | ~600 |
| 15:35 | Recorded binding architecture conventions from user's `.local/prompt.md` (views pattern, server-only pages, mocks, naming, theme-porting restriction) | `.wolf/architecture-conventions.md`, `.wolf/STATUS.md`, `.wolf/cerebrum.md`, `.wolf/anatomy.md` | done | ~1400 |
| 15:42 | Added convention: `src/types/` (`@/types/...`) holds reusable shared TS types | `.wolf/architecture-conventions.md`, `.wolf/STATUS.md`, `.wolf/cerebrum.md` | done | ~250 |
| 15:55 | Recorded MainLayout/Header porting logic precedent from user's prior project `medjana` (render order, shared navbarRoutes data, active-link helper, dropdown branching, reused CTA button) — structure only, not styling | `.wolf/architecture-conventions.md`, `.wolf/cerebrum.md` | done | ~700 |
| 16:10 | Ported Builty theme's DefaultHeader.js into Header.tsx + created MainLayout.tsx, navbar-routes/contact-info constants, NavRoute type, useLocalStorage hook, Logo ui component; wired into src/app/layout.tsx | `src/components/common/Header.tsx`, `src/components/layouts/MainLayout.tsx`, `src/@types/navigation.ts`, `src/constants/navbar-routes.ts`, `src/constants/contact-info.ts`, `src/hooks/useLocalStorage.ts`, `src/components/ui/Logo.tsx`, `src/app/layout.tsx` | done, tsc clean | ~3500 |
| 16:20 | Bug: ported Header looked unstyled (no grid/spacing) — root cause was vendor CSS (bootstrap/fontawesome/magnific-popup) linked via theme's `_document.js`, not carried over during port. Copied vendor CSS + webfonts into `public/`, linked in root layout head. Logged as bug-001. | `public/css/*`, `public/webfonts/*`, `src/app/layout.tsx`, `.wolf/buglog.json` | fixed | ~900 |
| 16:35 | Rebranded theme accent color `$accent-1` from `#ffee02` (yellow) to `#e65e24` (single SCSS variable, ~30 CSS rules affected); swapped text-based Logo for real `public/images/logo.png` (320x320, REEBAU wordmark baked in) via next/image | `src/styles/scss/variables.module.scss`, `src/components/ui/Logo.tsx` | done, tsc+eslint clean | ~400 |
| 16:45 | Filled in real social URLs (Instagram, TikTok) in socialLinks const, already wired into Header's off-canvas panel. Noted: user independently moved Header to `Header/index.tsx` (+ `style.css`), bumped Logo to 90x90, and updated `navbarRoutes` Services subRoutes to the real 6-service list (Bodenleger, Maler, Fliesenleger, Renovierer, Haustüren, Trockenbau) — differs from the original 5-service list in `.wolf/reebau-content.md`; that file is now stale on services and should be reconciled next time services content is worked on. | `src/constants/contact-info.ts` | done | ~200 |
| 17:00 | Bug: phone CTA icon in header invisible. Root cause: initial SVG port dropped `transform="translate(...)"` attrs on both paths along with the defs/clipPath/g wrapper they were removed from, so the small home-button rect jumped to the top and canceled visually with the phone outline. Restored both transforms on the paths directly. Logged as bug-002. | `src/components/common/Header/index.tsx`, `.wolf/buglog.json`, `.wolf/cerebrum.md` | fixed, tsc clean | ~500 |
| 17:05 | Found mobile-menu close button (`#res-cross`) was invisible because its CSS `background-image: url('/images/remove.svg')` pointed at an asset never copied from the theme. Copied `remove.svg` into `public/images/`. (User then paused this task to move to Footer work.) | `public/images/remove.svg` | fixed | ~150 |
| 17:20 | Ported theme's `DefaultFooter.js` → `Footer/index.tsx` (Server Component). Replaced the theme's address/phone/email "Contact" column with a "Nützliche Links" column (Services/Kontakt/Impressum/AGB/Datenschutzerklärung) per user request — interpreted "Contact info" as that column, not the footer-p-1 sales-rep block, which was kept (adapted to REEBAU phone + CTA, no stock photos since none exist for REEBAU). Copyright now `© {new Date().getFullYear()} Ree Bau – Powered By OrionLens` linking orionlens.net. Newsletter column ported structurally but left unwired (no real Mailchimp key) — flagged as open decision. Wired Footer into MainLayout after `{children}`. | `src/components/common/Footer/index.tsx`, `src/components/layouts/MainLayout.tsx`, `.wolf/STATUS.md` | done, tsc+eslint clean | ~2200 |
| 17:30 | Correction: 3rd footer column is not a Newsletter signup — it's a "request a callback" field (phone number + arrow-icon submit button, no visible button text). Changed input to `type="tel"`/"Ihre Telefonnummer", heading to "Rückruf anfordern", kept the original icon-only button (reverted an unwanted "Jetzt anfragen" text + CSS override attempt). Real ask was styling the **Nützliche Links** list instead — added `Footer/style.css` with link color + hover accent for `.footer-links li a` (user tweaked list margin/padding after). | `src/components/common/Footer/index.tsx`, `src/components/common/Footer/style.css` | done, tsc+eslint clean | ~600 |
| 17:40 | Added a hand-authored customer-service (headset) SVG icon next to the "Rufen Sie uns jetzt an" phone line in footer-p-1 (no matching icon existed in the theme's asset set, so drew a simple generic headset glyph using currentColor instead of fabricating/guessing an external asset). Spacing + accent color via Footer/style.css. | `src/components/common/Footer/index.tsx`, `src/components/common/Footer/style.css` | done, tsc+eslint clean | ~300 |
| 18:30 | Built out Home page sections from theme components: `Hero` (Swiper, Hero2.jsx), `HowWeWork` (Services.jsx shape, renamed per user, 3 items, links to /services), `About` (reverted by user back to the "our-goal" goal/team layout), `WhyUs` (Pricing.jsx shape repurposed as Zahlen & Fakten stats, since REEBAU has no pricing plans). Real project photos from user-provided `public/images/*` wired into Hero/About/WhyUs/HowWeWork, picked by visually reviewing each photo and matching to section theme (e.g. drywall install photos → Trockenbau content). | `src/components/views/Home/{Hero,HowWeWork,About,WhyUs}.tsx` | done, tsc clean | ~4000 |
| 18:50 | Bug pattern found twice: theme CSS often styles icons via `<img>`/`<svg>` selectors (e.g. `.svg-icon img`, `.service-two-icon svg,img`), not `<i>` font-icon tags — using Font Awesome `<i>` icons in those slots renders unstyled/invisible (no size/color/position). Fixed via scoped overrides in the shared `views/Home/style.css` (one file per view per `.local/prompt.md` convention, not per sub-component). For `.service-two-icon i` specifically, the theme already styles bare `i` as a hover-reveal arrow icon — had to add an extra `.service-main-icon` class to avoid colliding with that. | `src/components/views/Home/style.css`, `HowWeWork.tsx`, `Services.tsx` | fixed | ~600 |
| 19:00 | Built real `Services` section (Services2.jsx shape) as an ISR-ready pattern per user request: `src/@types/service.ts` (`Service` type), `src/mocks/services.ts` (5 real REEBAU services, single source), `views/Home/Services.tsx` takes `data: Service[]` as a **prop** (doesn't fetch/import mock itself) and slices to `data.slice(-5)` so it never renders more than 5 — `Home/index.tsx` (the future ISR fetch point) imports the mock and passes it down, simulating the real fetch→prop flow that will replace the mock later. | `src/@types/service.ts`, `src/mocks/services.ts`, `src/components/views/Home/Services.tsx`, `src/components/views/Home/index.tsx` | done, tsc+eslint clean | ~1200 |

## Session: 2026-08-09 08:43

| Time | Action | File(s) | Outcome | ~Tokens |
|------|--------|---------|---------|--------|
| — | Swapped Gallery's "what-we-build" hover-list design for the Projects.jsx Swiper-slider shape per user request; dropped `location`/`short` fields (user wanted title+image only), links all point to `/galerie`. Fixed equal-height Services cards (`height:100%` + `align-items:stretch`). Fixed a real theme bug: `.service-style-two` gets a black background in dark mode (`body.light-d`) but the theme never repaints title/CTA link colors for that state — added scoped `body.light-d ...` overrides in `views/Home/style.css`. Rebuilt Home's contact form to match a user-specified 5-field design (Name/Telefonnummer/E-Mail/Services-dropdown/Nachricht with visible uppercase labels) instead of the original minimal 2-field version; Services dropdown reuses `src/mocks/services.ts` (no duplicated data). Fixed Header's sticky-on-scroll (missing entirely after the initial port) via a `useEffect` scroll listener toggling `.is-sticky`, confined to Header's own files per user's explicit restriction. | `src/components/views/Home/Gallery.tsx`, `src/mocks/gallery.ts`, `src/@types/gallery.ts`, `src/components/views/Home/style.css`, `src/components/views/Home/ContactFaq.tsx`, `src/components/common/Header/index.tsx`, `src/components/common/Header/style.css` | done, tsc+eslint clean | ~3000 |
| — | Built `src/components/common/Breadcrumb/index.tsx` (ported from theme's `PageBanner.jsx`) as a reusable page-title/breadcrumb banner — takes `title` + optional `description`. First built the Über uns page from theme's `core-values.jsx`; user then asked to replace it entirely with theme's `about.jsx` sections instead (all except "Our Skilled Team" TeamSlider). Rebuilt `views/About/index.tsx`: 2-col intro (Wer wir sind / Warum REEBAU), stats counters, 4-step "So arbeiten wir" process (generic, since REEBAU never documented a real process), benefits checklist, closing CTA. Deliberately skipped `TestimonialSlider`/`PartnersSlider` (not requested to skip, but REEBAU has no real testimonials or partner logos — inventing fake customer quotes or partner logos would be fabricating reviews/endorsements) and flagged this in STATUS.md instead of faking content. | `src/components/common/Breadcrumb/index.tsx`, `src/components/views/About/index.tsx`, `src/app/ueber-uns/page.tsx`, `public/images/pattren-3.png` | done, tsc clean | ~2600 |
| — | Built Kontakt page (`/kontakt` → `views/Kontakt/index.tsx`, ported from theme's `contact.jsx`): contact form + info sidebar (real address/phone/email, real Instagram/TikTok socials) + FAQ accordion (extracted shared `src/mocks/faq.ts` + `src/@types/faq.ts` so Home's ContactFaq and Kontakt both reuse the same 4 questions instead of duplicating) + Google Maps embed built from the real street address via the `?q=<address>&output=embed` pattern (not fake coordinates like the theme's Pakistan demo embed). Skipped the theme's `.bio` director photo/name/signature block — no real named contact person exists, inventing one is the same fabrication risk as testimonials. Then, per user request, extracted the contact form's text fields into `src/constants/contact-form-fields.ts` (typed field config: name/phone/email/service/message) and a shared `src/components/ui/ContactFormFields.tsx` renderer (handles 2-col row chunking, the Services `<select>` sourced from `src/mocks/services.ts`, and textarea full-width) — both Home's `ContactFaq.tsx` and Kontakt's form now render the identical field set via this one component instead of hand-rolling inputs twice. | `src/app/kontakt/page.tsx`, `src/components/views/Kontakt/index.tsx`, `src/mocks/faq.ts`, `src/@types/faq.ts`, `src/constants/contact-form-fields.ts`, `src/components/ui/ContactFormFields.tsx`, `src/components/views/Home/ContactFaq.tsx` | done, tsc+eslint clean | ~3200 |
| — | Built Galerie page (`/galerie` → `views/Galerie/index.tsx`, ported from theme's `projects-2.jsx`, 2-col large-card grid). Confirmed prior agreement: `GalleryItem` has no `short` field. Per user request, clicking anywhere on a card opens a lightbox popup instead of navigating — built `src/components/ui/GalleryPopup.tsx` from scratch (no Magnific Popup JS package installed) reusing the exact `.mfp-*` class names from the already-loaded `magnific-popup.css` (bg/wrap/container/figure/close/arrows/counter) so it's visually consistent with the theme for free; driven by plain `activeIndex` React state + Esc/←/→ keyboard nav. Dropped the theme's non-functional demo pagination markup. | `src/components/ui/GalleryPopup.tsx`, `src/components/views/Galerie/index.tsx`, `src/app/galerie/page.tsx` | done, tsc+eslint clean | ~1600 |
| — | Ported theme's dynamic `services/[id].jsx` into `services/[slug]`. Checked installed Next.js version (16.3.0) against `AGENTS.md`'s warning and consulted `node_modules/next/dist/docs/` for the current dynamic-route API: `params` is now a `Promise`, used the global `PageProps<"/services/[slug]">` helper + `await props.params`. Extended `Service` type (+`image`, +`checklist: string[]`) and `src/mocks/services.ts` with real per-service checklists sourced from `.wolf/reebau-content.md`. Built `views/ServiceDetail/index.tsx` (Breadcrumb → hero image → intro → "Leistungen im Überblick" checklist via existing `.innovation ul` pattern) and `app/services/[slug]/page.tsx` (`generateStaticParams`, `generateMetadata`, `notFound()` on unknown slug). User then edited the hero image themselves from plain `<img>` to `next/image` with explicit width/height — kept as-is. | `src/@types/service.ts`, `src/mocks/services.ts`, `src/components/views/ServiceDetail/index.tsx`, `src/app/services/[slug]/page.tsx` | done, tsc+eslint clean | ~2000 |

## Session: 2026-08-10

| Time | Action | File(s) | Outcome | ~Tokens |
|------|--------|---------|---------|--------|
| — | Built reusable Legal page pattern per user-supplied reference (`.local/prompt.md` — a `LegalPage` component from a different past project, given only as a shape reference, not as a source of real content) adapted to REEBAU's own design system: new `LegalPage` component reuses existing `.about-first .who-we-are` typography (headings/paragraphs/icon-prefixed lists) instead of the reference's Webflow classes, so no new CSS was needed. Wired `/impressum`, `/agb`, `/datenschutz` (paths already matched Footer's existing "Nützliche Links"). Impressum uses real REEBAU data already on file (name, address, phone, email from `contact-info.ts`); did **not** carry over the reference project's fabricated-for-REEBAU-sounding fields (fake Geschäftsführer name, Handelsregister, USt-IdNr belonged to that other project's company, not REEBAU) — left an explicit "wird ergänzt" note instead per the standing no-fabrication rule, now extended to cover legal-entity data. AGB/Datenschutz got clearly-marked "wird erstellt" placeholders since the actual legal texts are substantive documents needing real content or legal review. | `src/@types/legal.ts`, `src/components/views/Legal/index.tsx`, `src/app/impressum/page.tsx`, `src/app/agb/page.tsx`, `src/app/datenschutz/page.tsx`, `.wolf/STATUS.md` | done, tsc+eslint clean | ~2200 |
| — | User supplied real content for two of the three legal pages, replacing the placeholders from the entry above. **Impressum**: real Name des Unternehmens/Firmensitz/Kontaktinformationen (incl. Webseite www.ree-bau.de) + Haftungsausschluss paragraph; Geschäfts-ID-Nr./Umsatzsteuer-Nr./Aufsichtsbehörde still show the user's own literal "Bitte geben Sie ... ein" placeholder text pending real values (not invented by AI). **Datenschutzerklärung**: full real 9-section DSGVO text (Verantwortlicher, erhobene Daten, Zweck/Rechtsgrundlage Art. 6 DSGVO, Weitergabe, Speicherdauer, Betroffenenrechte, Kontaktformular, Social Media, Änderungsvorbehalt), split into `LegalContent` paragraph/list blocks (address/phone/email interpolated from `contact-info.ts` rather than re-typed). AGB remains the only page still on a placeholder. | `src/app/impressum/page.tsx`, `src/app/datenschutz/page.tsx`, `.wolf/STATUS.md` | done, tsc+eslint clean | ~1400 |
| — | User explicitly asked for AGB content to be drafted, based on the same company info already used for Impressum/Datenschutz. Wrote 11 standard German Handwerks-AGB sections (Geltungsbereich, Vertragspartner, Vertragsschluss, Leistungsumfang, Preise/Zahlung — 14-Tage Zahlungsziel as a reasonable default, Termine, Mitwirkungspflichten, Gewährleistung — references gesetzliche Bestimmungen rather than inventing specific periods, Haftung, Kündigung, Schlussbestimmungen) using real company data (`contact-info.ts`) and only generic/statutory-default language, no invented business-specific facts. This is a deliberate one-time exception to the "never draft legal text without real input" stance — granted because the user explicitly requested it — not a precedent for other legal pages without asking first. Flagged in STATUS.md as boilerplate pending lawyer review, not presented as final. | `src/app/agb/page.tsx`, `.wolf/STATUS.md` | done, tsc+eslint clean | ~1100 |
| — | Built cookie-consent banner (`src/components/common/CookieConsent/`, own `style.css` since the theme has no matching component — bespoke CSS using the site's accent `#e65e24`/dark `#0f0f0f` palette). Reuses the existing `useLocalStorage` hook (key `cookie-consent`) to persist accept/decline and hide itself once decided. Wired into `MainLayout.tsx`. No tracking scripts exist yet to gate, so purely informational/consent-recording for now. | `src/components/common/CookieConsent/index.tsx`, `style.css`, `src/components/layouts/MainLayout.tsx` | done, tsc+eslint clean | ~900 |
| — | User opened the theme's `Layouts.js` and asked for the scroll-to-top button. Built `src/components/common/ScrollToTopButton/index.tsx` matching the theme's exact markup (`#scrollTop.scrollTopStick` + `fa-arrow-up`); the theme's CSS for it already existed in `_basic.scss` so no new styles were needed. The theme's actual show/hide + scroll behavior lives in an unopened `@common/scrollAnims` utility (out of scope), so reimplemented it directly using the same scroll-listener pattern as Header's sticky toggle (`scrollY > 300` threshold) + `window.scrollTo({behavior:"smooth"})` on click. Wired into `MainLayout.tsx`. | `src/components/common/ScrollToTopButton/index.tsx`, `src/components/layouts/MainLayout.tsx` | done, tsc+eslint clean | ~700 |
