# Memory

> Chronological action log. Hooks and AI append to this file automatically.
> Old sessions are consolidated by the daemon weekly.

## Session: 2026-08-08 15:02

| Time | Action | File(s) | Outcome | ~Tokens |
|------|--------|---------|---------|--------|
| 15:10 | Saved REEBAU website content into persistent AI-context file (source: gitignored `.local/contant.md`) | `.wolf/reebau-content.md` | done | ~950 |
| 15:20 | Recorded confirmed page structure (Home/Services/Galerie/Über uns/Kontakt + Impressum/AGB/Datenschutz), dynamic Services detail routes, and "Services" terminology decision | `.wolf/STATUS.md`, `.wolf/reebau-content.md`, `.wolf/cerebrum.md` | done | ~600 |
| 15:35 | Recorded binding architecture conventions from user's `.local/prompt.md` (views pattern, server-only pages, mocks, naming, theme-porting restriction) | `.wolf/architecture-conventions.md`, `.wolf/STATUS.md`, `.wolf/cerebrum.md`, `.wolf/anatomy.md` | done | ~1400 |
| 15:42 | Added convention: `src/types/` (`@/types/...`) holds reusable shared TS types | `.wolf/architecture-conventions.md`, `.wolf/STATUS.md`, `.wolf/cerebrum.md` | done | ~250 |
| 15:55 | Recorded MainLayout/Header porting logic precedent from user's prior project `medjana` (render order, shared navbarRoutes data, active-link helper, dropdown branching, reused CTA button) — structure only, not styling | `.wolf/architecture-conventions.md`, `.wolf/cerebrum.md` | done | ~700 |
| 16:10 | Ported Builty theme's DefaultHeader.js into Header.tsx + created MainLayout.tsx, navbar-routes/contact-info constants, NavRoute type, useLocalStorage hook, Logo ui component; wired into src/app/layout.tsx | `src/components/common/Header.tsx`, `src/components/layouts/MainLayout.tsx`, `src/@types/navigation.ts`, `src/constants/navbar-routes.ts`, `src/constants/contact-info.ts`, `src/hooks/useLocalStorage.ts`, `src/components/ui/Logo.tsx`, `src/app/layout.tsx` | done, tsc clean | ~3500 |
| 16:20 | Bug: ported Header looked unstyled (no grid/spacing) — root cause was vendor CSS (bootstrap/fontawesome/magnific-popup) linked via theme's `_document.js`, not carried over during port. Copied vendor CSS + webfonts into `public/`, linked in root layout head. Logged as bug-001. | `public/css/*`, `public/webfonts/*`, `src/app/layout.tsx`, `.wolf/buglog.json` | fixed | ~900 |
| 16:35 | Rebranded theme accent color `$accent-1` from `#ffee02` (yellow) to `#e65e24` (single SCSS variable, ~30 CSS rules affected); swapped text-based Logo for real `public/images/logo.png` (320x320, REEBAU wordmark baked in) via next/image | `src/styles/scss/variables.module.scss`, `src/components/ui/Logo.tsx` | done, tsc+eslint clean | ~400 |
| 16:45 | Filled in real social URLs (Instagram, TikTok) in socialLinks const, already wired into Header's off-canvas panel. Noted: user independently moved Header to `Header/index.tsx` (+ `style.css`), bumped Logo to 90x90, and updated `navbarRoutes` Services subRoutes to the real 6-service list (Bodenleger, Maler, Fliesenleger, Renovierer, Haustüren, Trockenbau) — differs from the original 5-service list in `.wolf/reebau-content.md`; that file is now stale on services and should be reconciled next time services content is worked on. | `src/constants/contact-info.ts` | done | ~200 |
| 17:00 | Bug: phone CTA icon in header invisible. Root cause: initial SVG port dropped `transform="translate(...)"` attrs on both paths along with the defs/clipPath/g wrapper they were removed from, so the small home-button rect jumped to the top and canceled visually with the phone outline. Restored both transforms on the paths directly. Logged as bug-002. | `src/components/common/Header/index.tsx`, `.wolf/buglog.json`, `.wolf/cerebrum.md` | fixed, tsc clean | ~500 |
| 17:05 | Found mobile-menu close button (`#res-cross`) was invisible because its CSS `background-image: url('/images/remove.svg')` pointed at an asset never copied from the theme. Copied `remove.svg` into `public/images/`. (User then paused this task to move to Footer work.) | `public/images/remove.svg` | fixed | ~150 |
| 17:20 | Ported theme's `DefaultFooter.js` → `Footer/index.tsx` (Server Component). Replaced the theme's address/phone/email "Contact" column with a "Nützliche Links" column (Services/Kontakt/Impressum/AGB/Datenschutzerklärung) per user request — interpreted "Contact info" as that column, not the footer-p-1 sales-rep block, which was kept (adapted to REEBAU phone + CTA, no stock photos since none exist for REEBAU). Copyright now `© {new Date().getFullYear()} Ree Bau – Powered By OrionLens` linking orionlens.net. Newsletter column ported structurally but left unwired (no real Mailchimp key) — flagged as open decision. Wired Footer into MainLayout after `{children}`. | `src/components/common/Footer/index.tsx`, `src/components/layouts/MainLayout.tsx`, `.wolf/STATUS.md` | done, tsc+eslint clean | ~2200 |
| 17:30 | Correction: 3rd footer column is not a Newsletter signup — it's a "request a callback" field (phone number + arrow-icon submit button, no visible button text). Changed input to `type="tel"`/"Ihre Telefonnummer", heading to "Rückruf anfordern", kept the original icon-only button (reverted an unwanted "Jetzt anfragen" text + CSS override attempt). Real ask was styling the **Nützliche Links** list instead — added `Footer/style.css` with link color + hover accent for `.footer-links li a` (user tweaked list margin/padding after). | `src/components/common/Footer/index.tsx`, `src/components/common/Footer/style.css` | done, tsc+eslint clean | ~600 |
| 17:40 | Added a hand-authored customer-service (headset) SVG icon next to the "Rufen Sie uns jetzt an" phone line in footer-p-1 (no matching icon existed in the theme's asset set, so drew a simple generic headset glyph using currentColor instead of fabricating/guessing an external asset). Spacing + accent color via Footer/style.css. | `src/components/common/Footer/index.tsx`, `src/components/common/Footer/style.css` | done, tsc+eslint clean | ~300 |
